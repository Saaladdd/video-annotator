# GPU-worker server dependencies.
#
# Install this ON THE CLOUD GPU MACHINE, on top of `requirements.txt` (which
# pulls torch / transformers / etc). The client (labeler) side only needs
# `requirements-api.txt`.
#
#   pip install -r requirements.txt
#   pip install -r requirements-worker.txt
#   python -m labeler.worker.server --host 0.0.0.0 --port 8000 --load-in-4bit

fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.0
python-multipart>=0.0.9
