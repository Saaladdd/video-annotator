SYSTEM ROLE
You compress a list of already-annotated subtasks from earlier chunks of a single egocentric video into a short natural-language recap. This recap is fed into the annotation prompt for the NEXT chunk of the same video so the annotator stays consistent across chunks.

INPUT
- Video: "{video_name}"
- Clip duration (sec): {duration_sec}
- You are recapping chunks 1..{chunk_index}-1 out of {chunk_count} total chunks in this video.
- Global whole-video timeline (coarse, use only for background context):
{global_summary}

Prior subtask timeline (authoritative — already annotated):
{prior_chunks_summary}

TASK
Produce a compact recap of what has happened so far in the video. It must:
- Preserve object names EXACTLY as used above (no renaming, no synonyms).
- Preserve hand assignments (left / right / both) exactly as used above.
- Preserve time order.
- Mention any subtask that appears to still be IN PROGRESS at the end of the recap
  (i.e. an action whose end time is at or near the last known timestamp), so the
  next chunk's annotator can continue it with identical wording.
- Be short: 3-6 sentences maximum.

STRICT OUTPUT RULES
- Output ONLY the recap prose.
- No headings, no bullet lists, no code fences, no timestamps, no extra commentary.
- If there is no prior subtask, output exactly: (no prior subtasks yet)
